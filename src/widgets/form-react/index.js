import React from 'react';
import ReactDOM from 'react-dom';

import Layout from './App';

const reactFormContainer = document.querySelector('.react-form-container');
ReactDOM.render(<Layout />, reactFormContainer);
